{
  "name": "validate-schema",
  "version": "1.0.0",
  "description": "Validate JSON/YAML files against schemastore.org schemas",
  "dependencies": {
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1"
  }
}

